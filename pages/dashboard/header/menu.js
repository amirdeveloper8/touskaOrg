import CreateMenu from "../../../components/header/create-menu/CreateMenu";
const Menu = () => {
  return (
    <section className="dashboard">
      <CreateMenu />
    </section>
  );
};

export default Menu;
